<div class="container">

  <div>
    <h1 class="my-4"><%= @header %></h1>

    <% if @items.empty? && params.has_key?(:query) %>
      <p>Your search returned no results</p>
    <% end %>

    <%= render 'items/item_card_grid' %>
  </div>
  
</div>
